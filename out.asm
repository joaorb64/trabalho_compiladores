
; >>Declaracao [n]
dseg SEGMENT PUBLIC ;início seg. dados
byte 4000h DUP(?) ;temporários
byte ? ;var. byte em 4000h
byte 100h DUP(?) ;var. string em 4001h
sword ? ;var. int em 4101h
byte ? ;var. boolean em 4103h
dseg ENDS ;fim seg. dados

; >>Declaracao [nome]
dseg SEGMENT PUBLIC ;início seg. dados
byte 4000h DUP(?) ;temporários
byte ? ;var. byte em 4000h
byte 100h DUP(?) ;var. string em 4001h
sword ? ;var. int em 4101h
byte ? ;var. boolean em 4103h
dseg ENDS ;fim seg. dados

; >>Declaracao [naoTerminou]
dseg SEGMENT PUBLIC ;início seg. dados
byte 4000h DUP(?) ;temporários
byte ? ;var. byte em 4000h
byte 100h DUP(?) ;var. string em 4001h
sword ? ;var. int em 4101h
byte ? ;var. boolean em 4103h
dseg ENDS ;fim seg. dados

; >>Declaracao [x]
dseg SEGMENT PUBLIC ;início seg. dados
byte 4000h DUP(?) ;temporários
byte ? ;var. byte em 4000h
byte 100h DUP(?) ;var. string em 4001h
sword ? ;var. int em 4101h
byte ? ;var. boolean em 4103h
dseg ENDS ;fim seg. dados

; >>Declaracao [a]
dseg SEGMENT PUBLIC ;início seg. dados
byte 4000h DUP(?) ;temporários
byte ? ;var. byte em 4000h
byte 100h DUP(?) ;var. string em 4001h
sword ? ;var. int em 4101h
byte ? ;var. boolean em 4103h
dseg ENDS ;fim seg. dados

; >>Declaracao [y]
dseg SEGMENT PUBLIC ;início seg. dados
byte 4000h DUP(?) ;temporários
byte ? ;var. byte em 4000h
byte 100h DUP(?) ;var. string em 4001h
sword ? ;var. int em 4101h
byte ? ;var. boolean em 4103h
dseg ENDS ;fim seg. dados

; >>Declaracao [b]
dseg SEGMENT PUBLIC ;início seg. dados
byte 4000h DUP(?) ;temporários
byte ? ;var. byte em 4000h
byte 100h DUP(?) ;var. string em 4001h
sword ? ;var. int em 4101h
byte ? ;var. boolean em 4103h
dseg ENDS ;fim seg. dados
